# Copy this file to .env and fill in values as needed
# OpenAI API key (optional). If omitted, server will use mock AI responses.
OPENAI_API_KEY=

# Server port override
PORT=4000
HOST=0.0.0.0

# SQLite database URL (enable persistence). Comment out to use in-memory fallback.
DATABASE_URL="file:./dev.db"

# Enable real OCR with tesseract.js (1 = on, else mock)
USE_TESSERACT=0
